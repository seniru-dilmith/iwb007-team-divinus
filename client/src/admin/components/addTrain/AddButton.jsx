import React from 'react';
import { Button } from 'react-bootstrap';

const AddButton = () => {
  return (
    <Button type="submit" className="add-train-btn">
      Add
    </Button>
  );
};

export default AddButton;
